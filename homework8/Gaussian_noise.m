image = imread('lena.bmp'); 
figure;
imshow(image); 
[height,width] = size(image); 
amp = 30;
X = randn(512,512); %??????????????(????????? 512*512 ???? ? ???? 0????? 1)

N = amp * X; 
for i = 1 : height
    for j = 1 : width
        image(i,j) = image(i,j)+N(i,j);
    end
end
figure;
imshow(image); 
imwrite(image,'gau_noise2.bmp');